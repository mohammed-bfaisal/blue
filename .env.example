# BLUE — Environment Variables
# Copy to .env.local and fill in values

# Optional: Supabase credentials for cloud sync
# Leave blank to run fully local (PGlite/IndexedDB)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
